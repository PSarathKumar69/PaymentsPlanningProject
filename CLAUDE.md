# Vendor Payment Planning & Prioritization Automation

Context file for Claude Code. Read this first on every session. Detailed specs live in `docs/` — read the relevant file before touching related code.

## What this project is

A tool that helps a Finance team decide, every payment cycle, how much to pay to which vendors — especially when available funds are less than total amounts owed. The tool generates suggested payment plans. **It never executes payments and never overrides Finance's judgment.** Finance reviews suggestions and makes the final call, every time.

Full background: `docs/01-objective-and-current-process.md`

## Stack

- Backend / model logic: **Python**
- Frontend: **React**
- Data input: Excel — the real master sheet (`data/Vendor's Details.xlsx`, project root) is already available and is what all code should be built/tested against — see `docs/07-data-pipeline-and-master-sheet.md`
- Database: **SQLite**, accessed through an ORM (not raw SQL), to keep a Postgres migration path open. See `docs/12-database.md` for the decision, reasoning, and rough table shape.

## Non-negotiable rules (apply to all code)

1. **Ledger integrity**: `Closing Balance = Opening Balance + Total Payable − Total Payment` must always reconcile exactly. Never write logic that can produce a negative balance or a payment greater than what's outstanding. Full detail in `docs/07-data-pipeline-and-master-sheet.md`.
2. **Suggestion-only**: nothing in this codebase ever calls a real payment/bank API. Every model's output is a proposed plan a human reviews.
3. **Deterministic vs AI layer — hard boundary**: all arithmetic (aging, scores, fund allocation, splits) happens in plain, deterministic Python code that reads the real vendor data directly. Any AI/LLM component only reads the deterministic layer's already-computed output and explains it in plain language — it must never receive raw vendor data directly or perform its own arithmetic on it. See `docs/02-architecture.md`.
4. **New Model 2's funds figure comes first — initial monthly plan only**: this is two distinct UI actions, not one button. (a) A "Calculate Minimum Funds Required" action runs the minimum-funds calculation and displays the number — no plan exists yet. (b) Only after that number is shown does the funds-input field become usable; Finance enters an amount, then a separate "Generate Plan" action produces the actual plan. Never build a flow that asks for funds before showing this figure. This two-step sequence applies **only** to the initial monthly plan — mid-month regeneration does not recalculate or redisplay the minimum-funds figure (see `docs/06-weekly-planning-regeneration.md`). See `docs/14-new-model-2.md`.
5. **P0 and P1 vendors (Must Pay / Commitment) are funded via their own configurable ceiling/floor, seeded at 100%/100% by default (P0/P1-ceiling task, 2026-08-06) — no longer a hardcoded bypass.** At that untouched default, behavior is unchanged from before: fund shortages only ever affect P2/P3/P4 (Normal) and P5 (Inactive) — P5 was added 2026-07-24 as its own independent, cuttable bucket (cut first in a shortfall, its own ceiling/floor), no longer folded into P4 the way the old Exit tag was. Don't write logic that hardcodes P0/P1 as unconditionally exempt from the funds constraint — read their ceiling/floor from `priority_buckets` like every other bucket. **Their bucket_key/category_name/rotation_position edit-lock was removed (2026-08-07 task, Sarath's explicit call, reversing the original "can't be changed" clause this rule used to have)** — P0/P1 are now editable/renamable/reorderable through `priority_bucket_edits.py` exactly like every other bucket row; the Configuration UI no longer shows them as pinned/locked for those fields. **Identity is decoupled from label (pinned-role task, same day, fixing the sharp edge the edit-lock-removal task had flagged and deliberately accepted)**: `PriorityBucket` has its own `pinned_role` column (`"must_pay"`/`"commitment"`/`NULL`, backfilled once from whichever row was literally `bucket_key=="P0"`/`"P1"` at the time), and `allocator.py`'s guaranteed-funding-tier detection reads that column, never a literal `"P0"`/`"P1"`/`"must_pay"`/`"commitment"` string comparison — renaming a bucket_key or category_name no longer moves vendors out of the guaranteed tier, and `_seed_and_read_buckets()` checks `pinned_role` (not bucket_key) before reseeding, so a rename can't spawn a phantom duplicate default row either. The one thing that's still locked: `remove_bucket()` refuses to delete a `pinned_role` row outright, in-use or not — deleting it would remove the actual guarantee, not just relabel it. Don't hardcode `"P0"`/`"P1"` as the guaranteed-tier check anywhere new; resolve it from `pinned_role` (backend) or the `deletable` field `GET /config/priority-buckets` returns (frontend) instead.
6. **Configuration edits write back to Excel, and get audited.** Any vendor data/tag edit made in the UI must write back into the actual Excel file — it's the single source of truth, never a separate store that can drift from it. Every override (UI edit or replacement Excel upload) is recorded in a separate append-only audit log. See `docs/11-configuration-module.md`.
7. **No hardcoding — config/DB-driven, not literals in code.** New Model 2's bucket ceiling percentages (P0/P1/P2/P3/P4) must be read from the `config` table (`docs/12-database.md`), never hardcoded as literals inside allocation functions — Finance can change these through the Configuration module, and the code must pick that up without a redeploy. Status/state values (vendor category, priority tag, vendor payment status, plan status, Reconsider Yes/No, etc.) must be defined once in a single shared enum/constants module and referenced everywhere, never re-typed as ad hoc strings scattered through the codebase. Excel column mapping must go through a configurable mapping layer, not fixed column letters/positions — the real sheet's exact layout is still an open item (`docs/9-open-questions.md`). The number of weeks in a month (W1-W5) must be derived from the calendar, not hardcoded to always be 5. Frontend components fetch data through the API service layer (`docs/02-architecture.md`) — never embed sample/mock data directly in component code once real endpoints exist.

## The planning model

Finance and leadership have finalized **New Model 2 — Priority-Bucket Ceiling Allocation** as the single planning model. Earlier candidate models (Old Model 1/2/3, New Model 1) were explored and retired during design; they are no longer part of this codebase and their docs have been removed. Do not reference or resurrect them.

- **New Model 2 — Priority-Bucket Ceiling Allocation**: `docs/14-new-model-2.md` — vendor categories (Must Pay/Commitment/Normal/Inactive) and priority tags (P0-P5), bucket ceilings, minimum funds required, allocation logic, leftover funds top-up, overrides/locking, weekly view.

## Weekly planning & regeneration

The monthly plan splits into weekly sub-plans (W1-W5), and Finance can regenerate mid-month with specific include/exclude rules. This is easy to get subtly wrong — read `docs/06-weekly-planning-regeneration.md` in full before implementing any plan-generation or regeneration logic.

## Current project stage

MVP, actively being built out feature by feature. Implemented and verified so far: New Model 2's core allocation logic (bucket ceilings, cutting/rotation, leftover funds top-up), the Exit vendor category/priority tag, and the data-pipeline Excel upload flow (single-button upload/replace, one-slot backup + revert, generic passthrough fields for unmapped columns with auto-detected widgets). **Resolved (Main-tab revamp task)**: the earlier "`MasterDataGrid.tsx` is dead code" note (2026-07-29) no longer applies — it's now imported and rendered on the Main tab (full width, below a new 50/50 upload-card/upload-history row), polished (sticky header + frozen ERP Code/Vendor Name columns, zebra striping, a vendor/ERP-code text filter), and driven by its own `refreshSignal` bumped on upload/revert/rollover. `PlanningView.tsx`'s own table remains the vendor grid Finance actually plans against — `MasterDataGrid.tsx` is the read-oriented Master Data view, not a replacement for it. Same task also changed the upload flow itself: picking/dropping a file now opens an `UploadConfirmModal` (Planning Month + a secondary, auto-detected Sheet start month field) instead of committing immediately — Confirm & Upload calls the same `commitUpload()`, Cancel discards with no request made. Planning Month is now confirmed once here, not re-asked on the Planning tab (see below).

**Resolved (merge-rollover-into-upload task)**: Month-end rollover is no longer a separate "Month-end rollover" button/action — folded into every upload instead, since Finance should never have to remember which button to click. `POST /master-data/commit-upload` (`backend/ingestion/upload.py::commit_upload()`) now captures the DB's latest ledger month before its own `load()` call and compares it to the sheet's latest month after; only on a genuine month advance does it call the extracted `backend/month_end/rollover.py::_reset_vendor_cycle_state()` (payment_status/paid_so_far_this_month/reconsider/override_amount/week_distribution_plan/finalized_budget_amount), in the same transaction as the ingestion — a failure during the reset rolls back the ingestion too. Unlike the old standalone `rollover_month()`, an ordinary same-month correction re-upload (now the routine case, since every re-upload takes this one path) silently skips the reset and never raises — that refusal framing belonged only to the old separate-button action. The old `rollover_month()`, its `/rollover` router/endpoint, and the frontend's separate button/confirm-modal/`postRollover()` call were all removed outright (nothing else called them). `commit-upload`'s response now carries `cycle_reset`/`vendors_reset`, surfaced in the upload toast only when a reset actually happened.

The current cycle's confirmed Planning Month is now a real persisted value, not just recoverable from the latest `PlanRun` row — `backend/shared/planning_month_store.py` (`get_current_planning_month()`/`set_current_planning_month()`, same Config-backed pattern as `column_mapping_store.py`'s sheet-start-month functions). `POST /master-data/commit-upload` accepts an optional `planning_month` field and persists it there; `new_model_2.py`'s `_resolve_planning_month()` now falls back to it (after the latest `PlanRun`, before raising) so a value exists even before the first Generate Plan of a cycle. New `GET /models/5/current-planning-month` exposes that resolved value read-only — the upload modal uses it to pre-fill Planning Month (falling back to `suggested-planning-month` if nothing's set yet), and `PlanningView.tsx`'s own Planning Month card now just displays it instead of taking input. `GET /models/5/minimum-funds-required`'s `planning_month` param is optional now too, resolved the same way — existing callers that always pass it explicitly are unaffected.

**Done since the last update**: the explicit Finance-picked planning month (month+year, no day, asked once per cycle) and the new capped Minimum Funds Required rule (oldest outstanding month vs. 50% of the current month's own bill, at most 2 months, never an unbounded leftover chain) are both implemented — `backend/shared/min_funds_v2.py` (`required_amount_v2()`/`min_funds_breakdown_v2()`/`calculate_minimum_funds_required_v2()`) plus `backend/api/routers/new_model_2.py`'s planning-month plumbing and its own `/models/5/minimum-funds-required` endpoint. `backend/shared/min_funds.py`'s original `required_amount()`/`_carry_forward_normal_amount()` were left untouched, as required — see below for what those are actually used for now.

**Old Model 1/2/3 and New Model 1's own routers, schemas, and model packages have been removed from the codebase** — `main.py` only registers New Model 2 + shared routers today. The vendor-scoring module that used to live under an old-model-named directory has been relocated: `backend/shared/scoring.py` (`score_vendors()`/`recompute_and_persist_scores()`/`read_weights()`, formerly `backend/models/model1_weighted_scoring/scorer.py`) is now framed as shared infra — `backend/shared/payment_logging.py` (vendor score/aging-bucket recompute on payment) and `backend/weekly_planning/planner.py` both call into it live, for New Model 2. The now-dead `MODEL_1_PLAN_RUN_LABEL`/`NEW_MODEL_1_PLAN_RUN_LABEL` constants, the dead old-model tab calls in `test_ui.html` (which now shows New Model 2 only, no tab bar), and the test-pipeline/demo-script artifacts (`testpipeline/`, `scripts/seed_demo.py` and friends, `backend/db/app.db`/`demo.db`) have all been removed too — the DB files are regenerated fresh (ingestion re-run against the real master sheet for `app.db`; `demo.db` left empty, to be reseeded later once the React UI is finished). **`backend/shared/min_funds.py`, and its use in `vendors.py`, was explicitly left alone** (only stale comments reworded) — that's live, shared code `vendors.py`'s own vendor list/aging endpoint still depends on, not an old-model leftover, despite older comments in that file framing it that way. `planner.py` still uses this module's `_load_vendors_and_ledger()` (unchanged), but no longer its `required_amount()` — see below.

**Resolved (this task)**: the Planning-table-vs-Weekly-view Min-Funds discrepancy flagged below for several tasks is now fixed. `backend/weekly_planning/planner.py`'s weekly-view builder now calls `required_amount_v2()` (`backend/shared/min_funds_v2.py`) instead of the old `required_amount()` (`backend/shared/min_funds.py`) — the only production caller of `build_weekly_view()` is New Model 2's own generate-plan-and-weekly-view flow (Models 1/2/3/New Model 1 are gone), so this was safe to switch with no other model affected. Planning table and Weekly view now agree on Min Funds Required for the same vendor. `backend/shared/min_funds.py`'s `required_amount()` itself was not touched — `vendors.py`'s vendor list/aging endpoint is still its only live caller.

**Resolved (P1 demo-readiness task, ahead of the leadership demo against a real Finance-provided sheet)**: `SHEET_START_MONTH` is no longer a bare module constant in `backend/ingestion/column_mapping.py` — it's now `backend/ingestion/column_mapping_store.py`'s Config-backed `get_sheet_start_month()`/`set_sheet_start_month()` (seeded with the same `2025-04-01` default on first read, same pattern `backend/shared/scoring.py`'s weights already use), read fresh by every real ingestion call path (`load_excel.py::load()`, `grid.py::build_master_grid()`, `vendor_edits.py`'s Excel write-back). `build_sheet_map()` takes an explicit `sheet_start_month` parameter (defaulting to the seed constant only for callers with no DB access, e.g. tests) rather than reading a global. A new non-blocking check, `column_mapping.sheet_start_month_warning()`, cross-checks each payable/payment column's own header text against its position-derived month and surfaces a loud (but never blocking) banner on `POST /master-data/commit-upload` if 2+ columns disagree (the sheet's one confirmed real typo alone never fires it) — same `ai_column_mapping_messages` banner array `MainTab.tsx` already renders. Stretch goal also done: `commit-upload` accepts an optional `sheet_start_month` field (`MainTab.tsx`'s small optional "Sheet start month" input), which both drives that upload and persists to `config` for every future one.

Also fixed in the same task: `header_overrides` (the AI-resolved column mapping) is now threaded through **every** `build_sheet_map()` call site with DB access — `vendor_edits.py`'s Excel write-back and `grid.py`'s Master Data grid were building a sheet map with no override at all, so a real sheet needing an AI-resolved header for a required field would ingest fine but then crash (`MissingRequiredColumnsError`) the moment Finance edited a vendor field or opened the grid. Covered by `backend/ingestion/test_header_override_threading.py`. Separately, `MasterDataCommitResponse` (`backend/api/schemas/master_data.py`) was missing its own `ai_column_mapping_messages` field — `commit_upload()` has always returned that key, but FastAPI's `response_model` silently drops any key a Pydantic model doesn't declare, so this banner never actually reached the frontend through the real HTTP endpoint before this fix (only direct in-process test calls ever saw it).

Several architecture and data questions are still open — **do not assume answers to these**, check `docs/9-open-questions.md` and flag if a task depends on one of them (`docs/9-open-questions.md` itself currently says nothing is open). Stale claim noticed in passing during this task, not reconciled here (out of scope): this section used to say the "within-week execution order" rule in `docs/06-weekly-planning-regeneration.md` was still flagged for Sarath's confirmation — but `docs/06` itself has since been updated with a confirmed answer (priority-tag order, then the shared vendor score as a tiebreak), so that line has been dropped here. `docs/18-project-status-tracker.md`'s own "Needs Sarath's decision" section still has the same stale claim and should be reconciled too next time someone's in there.

**React UI (`frontend/`, Vite + React + Tailwind) is now fully wired to real endpoints**, not mock data — `frontend/src/api/*.ts` calls the real backend across Main (data pipeline/upload), Planning (the full New Model 2 flow — calendar picker, Min Funds card, Available Funds, generate/regenerate, vendor table with priority-tag filters and P5 support, vendor detail modal), and Configuration (priority-bucket CRUD) tabs. Analytics was a placeholder as originally planned, but is now a real, backend-wired page — see the resolved entry below. See `docs/15-react-ui-layout.md` — its "mock-data phase" section describes a now-completed, temporary state, not the current one. `test_ui.html` still exists as a secondary reference dashboard (it predates the React UI); the queued cleanup handoff above trims its dead old-model tabs but doesn't remove it outright.

**Resolved (Master Grid follow-up task)**: the grid no longer renders unconditionally — `GET /master-data/grid` (`backend/ingestion/grid.py::build_master_grid()`) returns a `has_data: false` sentinel (never a raw crash) when no vendor has ever been ingested yet or the master file is missing, judged by active-vendor count first (cheaper, and correct even if a stale file sits at `EXCEL_PATH` with an empty DB). `MasterDataGrid.tsx` shows a plain "Upload a file to see your master data here" placeholder instead of the table until real data exists.

**Resolved (intelligent Category<->Priority Tag derivation task)**: the real sheet's own "Category" column (`Vendor`/`Non Vendor`) turned out to hold a different vocabulary entirely, not Finance's Must Pay/Commitment/Normal/Inactive labels — every vendor was silently defaulting to `Normal` with zero warning. `column_mapping.py`'s `category_from_label()` (alias-normalized: case/space/hyphen variants) + `derive_category_and_priority_tag()` now treat category and priority tag as two views of one signal, deriving whichever is missing (`P0`->Must Pay, `P1`->Commitment, `P2`/`P3`/`P4`->Normal, `P5`->Inactive, and the reverse — `Normal`->`P2` confirmed default). Confirmed-with-Sarath conflict rule: if both are present and disagree, the category label wins, logged loudly (`category_priority_conflict` audit entry, never silent). The same derivation now also applies to single-field UI edits (`vendor_edits.py`'s `_derive_sibling()`) — editing Category or Priority Tag individually auto-updates the sibling field, writes both to their own Excel columns in one save cycle, and logs the sibling change as its own distinct `ui_edit_derived` audit entry. A Commitment-category edge case is flagged (non-blocking warning, not enforced) when `commitment_months` isn't set on a newly-Commitment vendor, since that field drives a completely different required-amount formula (`min_funds.py`, untouched, still Commitment's own path). Proven end-to-end: `generate_plan()` genuinely plans differently (guaranteed vs. bucket-ceiling-cut) after such an edit, not just a display change.

**Resolved (duplicate ERP code + vendors.py percentage task)**: two real bugs found investigating a live discrepancy (Finance paid 100% of a suggested amount, Payments table showed 89.96%). First, `backend/api/routers/vendors.py`'s payment-tracking endpoint was calling the old, retired-model-era `required_amount()` (`min_funds.py`) instead of New Model 2's own `required_amount_v2()` — swapped, matching the fix `planner.py` already had for this exact bug class. Second, 29 ERP codes appearing twice in the real sheet (unrelated vendors sharing a code by data-entry error) were silently collapsing to one Vendor record on ingestion, with zero trace — confirmed with Sarath: keep the last-row-wins behavior, but warn loudly (`duplicate_erp_code_notes`, surfaced on the upload banner), never block.

**Resolved (test-isolation audit, third occurrence of this bug class)**: `test_vendor_edits.py` was found to be running "for real" against the actual production `backend/db/app.db` (`session=None`), asserting a stale hardcoded prior-value assumption, and its `finally` block force-reverting the vendor to that same stale value regardless of whether the assertion passed — this had genuinely corrupted a real vendor's category in production, deleting the audit-log entry for the change in the same revert. Rewrote the file onto the same `_fresh_db_and_master(tmp_path)` isolation every other test file already uses (removing the "run for real, then revert" pattern entirely, not just patching the hardcoded value), audited all 19 `SessionLocal`/`load()`/`commit_upload()`/`update_vendor_field()`-touching test files (only this one had the gap), and added `backend/conftest.py` — an autouse fixture for every test under `backend/` that raises immediately if any test actually commits against the real DB or overwrites the real Excel/backup file, regardless of which test does it.

**Resolved (AI column-mapping transport-failure task)**: a TLS handshake killed by a proxy/firewall produces `httpx.ConnectError`, which is not a `genai_errors.APIError` subclass — `ai_column_mapper.py::map_columns()` only caught the latter, so a connection-level failure fell through uncaught and crashed the whole upload as a raw 500 instead of the existing clean 503 path. Now catches `(genai_errors.APIError, httpx.HTTPError)`, the latter being the base class covering connect/timeout/TLS failures too.

**Resolved (Generate Plan 400 + Master Grid datetime headers)**: a ledger row dated at/after the resolved `as_of` (a real case whenever Finance's planning month isn't exactly "sheet's last month + 1") sent `bucket_for_months_back()` (`backend/shared/aging.py`) a negative months-back offset, which it correctly rejects — the `ValueError` escaped uncaught through `build_weekly_view()` and got turned into a misleading 400 by `main.py`'s global handler, even though `generate_plan()` itself had already succeeded. Fixed by clamping (`max(0, ...)`, flagged house-style decision: the planning month's own cycle is "being funded, not aged against itself," so excluding the row outright risked silently dropping real money) rather than excluding — `payable_by_mb`/`payment_by_mb`/`remaining_by_months_back` now accumulate instead of overwrite so two real months clamping to the same bucket sum correctly. Separately, some real header cells in the Payable/Payment monthly blocks are literal `datetime`-typed cells among plain-string neighbors, so Master Grid showed raw `"2026-04-26 00:00:00"`-style headers — fixed with a new `format_header_value()` (`column_mapping.py`) wired into both `grid.py` and `unmapped_header_columns()`. Independently reproduced the real 400->200 fix against a copy of the real DB; full-suite regression claim (71 failed/329 passed, all pre-existing) only spot-checked, not fully re-run — this sandbox can't run the whole suite in one pass.

**Resolved (AI Screen revamp)**: `CompanionPanel.tsx`'s floating-widget talking points and `VendorDetailModal.tsx`'s separate inline zero-status panel (which called `/ai/talking-scripts`) are now one shared `frontend/src/components/AiCompanionCard.tsx`. `POST /ai/vendor-talking-points`'s response now also carries `category`/`priority_tag`/`required_amount`/`allocated_amount`/`aging_bucket` (already computed by the fact-pack, previously discarded), so the card shows a header/key-facts row/aging chip alongside the AI narrative, plus Copy/Regenerate and a loading skeleton. `postTalkingScripts` removed as dead code from `frontend/src` (backend route untouched, `test_ui.html` still calls it). **Known gap, not yet fixed**: the floating-FAB entry point (`CompanionPanel.tsx`) still opens the card with `variant="popup"` — the old small ~384px corner style — rather than the bigger centered `"modal"` variant Sarath asked for; only `VendorDetailModal`'s "Ask AI" link (default variant) got the new bigger card. The FAB itself is bigger as asked, but the card it opens didn't change shape.

**Resolved (Finalize shortfall warning, final iteration)**: the earlier `ShortfallModal.tsx` (3 suggested actions, its own popup) was deleted outright, then a standalone red banner on the Planning page replaced it, then that banner itself moved *inside* the "Finalize this plan?" confirm popup — `ConfirmModal.tsx` gained a new `extra?: React.ReactNode` prop, and `PlanningView.tsx` now renders the "Short by ₹X" warning + a suggestion line there via that prop. Finalize/Cancel both remain available in the same popup — Finance is warned, not blocked. `ShortfallModal.tsx` and its test file are confirmed deleted.

**Resolved (AI Talking Points — currency fix + Talking/Email toggle)**: `backend/ai_layer/prompt_template.py`'s shared guardrails now hard-require the ₹ symbol with Indian digit grouping and explicitly ban `$`/USD/"dollars" (fixes a real bug — Gemini was writing dollar amounts for an Indian Rupee business). Also added a banned-phrasing guardrail (no "our current operational plan," "aging period," etc. — vendor-facing copy shouldn't explain internal reasoning). `AiCompanionCard.tsx` now has a Talking/Email segmented toggle backed by `POST /ai/vendor-talking-points`'s new `format` field (`"talking"`/`"email"`, `backend/ai_layer/companion.py`'s `_PROMPT_BUILDERS` dict), each tab lazily fetched and cached independently.

**Resolved (two new Excel downloads on the Planning tab)**: `GET /models/5/finalized-plan-export` (a "Download Excel" button beside Finalize Plan, enabled once at least one vendor has a finalized Budget) downloads the real master workbook — via a temp copy, `EXCEL_PATH` itself is never touched — with two new appended columns, Suggested Plan Percentage and Suggested Plan Amount (blank, not 0, for any vendor never finalized this cycle). `GET /models/5/min-funds-verification-export` (a "Verify Min Funds" button between Generate Plan and Finalize Plan, enabled once Min Funds has been calculated) downloads a brand-new workbook, unrelated to `EXCEL_PATH`, with Vendor Name / Outstanding / one column per aging window (0-30, 31-60, ... uncapped past 120 days, however far the worst-aged vendor needs) / Min Funds Required — so Finance can manually re-check the system's own math.

**Resolved (negative-payment bug, found via the Verify Min Funds export)**: a real Finance sheet has 7 vendors with a negative value in a Payment cell some month (e.g. CAPSTECH NETWORK PVT LTD, −₹332,000 in June 2026) — this surfaced because Outstanding and the aging-bucket columns disagreed by exactly that amount in the downloaded verification sheet. Root cause was two different, both-wrong reactions to the same cell: `load_excel.py`'s running-balance walk (`closing = opening + payable − payment`) turned a negative payment into *added debt* (subtracting a negative adds it back), while `aging.py`'s FIFO tranche walk silently discarded it entirely (`if payment <= 0: break`). **Confirmed with Finance**: a negative payment cell means an over-payment recorded with the wrong sign, and should reduce the balance like any real payment — never add debt, never get ignored. Fixed at the single source: `load_excel.py` now flips a negative payment to `abs(payment)` before it's stored in `MonthlyLedger.payment` (with a `data_quality_notes` flag surfaced on the upload banner), so every downstream reader (aging, min-funds, `vendors.py`) sees one already-corrected value — no separate fix was needed in `aging.py`/`min_funds.py` since both only ever read `MonthlyLedger.payment`, never a raw sheet value independently.

**Resolved (Upload History card removed)**: Sarath's call — the card added no value Finance used. `MainTab.tsx`'s upload card is now full-width; "Revert to previous upload" moved into its top-left corner; the AI column-mapping warning banner now shows for the most recent upload only, above the Master Data grid.

**Resolved (45-vendor synthetic end-to-end model validation)**: built to directly answer Sarath's doubt about whether the bucket-ceiling/round-robin allocation is actually working — a dedicated `test_data/` dataset (7 Must Pay, 14 Commitment, 19 Normal split across P2/P3/P4, 5 Inactive) plus `backend/validation/test_new_model_2_e2e_validation.py` (8 tests), run across fully-funded/moderate-shortfall/severe-shortfall scenarios with predictions computed independently before generating the real plan. One real bug found and fixed along the way: `leftover_topup_total` was computed correctly by `allocator.py` but silently stripped from the API response — `NewModel2GenerateResponse` didn't declare the field, same failure class as the earlier `ai_column_mapping_messages` bug (FastAPI's `response_model` silently drops undeclared dict keys).

**Resolved (Analytics tab built for real, not locked)**: Sarath had a full Analytics UI (KPI cards, aging visual, Chart.js charts, per-vendor table with month-by-month history, vendor detail modal) built externally in Google AI Studio with no backend. Moved into `frontend/src/components/VendorAnalyticsTab.tsx`, wired into the sidebar in place of the placeholder (the earlier "lock this tab" plan is superseded — we built the real thing instead). Backend: `backend/analytics/calculations.py` is the single home of every Analytics calculation (`get_analytics_dashboard()`/`get_funds_trend()`), delegating to `compute_vendor_aging()`/`live_outstanding_balance()` rather than reimplementing them; `backend/api/routers/analytics.py` is thin wiring only (`GET /analytics/dashboard`, `/analytics/funds-trend`, `/analytics/export`). Three decisions baked in: the funds-required/available-funds trend only shows months with a real `PlanRun` row (nothing backfilled/invented for months before Sarath started using this tool); the current/open month's "Paid" figure uses live `vendor.paid_so_far_this_month`, not the ledger's (possibly stale) `MonthlyLedger.payment`; the mockup's "Liquidity Health" badge was dropped entirely, not reimplemented. `analyticsMockData.ts` deleted. New persistence needed along the way: `PlanRun.min_funds_required` (nullable), populated at Generate Plan time via `calculate_minimum_funds_required_v2()`, since this figure had never been stored anywhere before.

**Resolved (Funds Left card wrong after any Planning-tab remount)**: `PlanningView.tsx`'s `handleGeneratePlan()` correctly set Funds Left from `allocator.py`'s real `leftover_remaining` right after Generate/Regenerate, but `loadAll()` (runs on every remount — tab switch away and back, or a reload) overwrote it with a flawed approximation (`Available Funds − total overrides`, ignoring what the bucket-ceiling allocation actually spent on every other vendor) — the same bug class an earlier task already fixed once under a different name, just reintroduced in this separate code path. Root cause: `leftover_remaining` was never persisted, only ever returned once in the generate-call response. Fixed the same way as `min_funds_required` above: added `PlanRun.leftover_remaining` (nullable), populated in the same update call, exposed through `PlanRunOut`/`build_plan_run_history_response()` (both were missing `min_funds_required` too — same "FastAPI silently drops undeclared response fields" class hit before, fixed alongside). `loadAll()` now reads `latestRun.leftover_remaining` directly; old pre-fix `PlanRun` rows show "—" (via `formatMoney(null)`) rather than a fabricated number.

**Resolved (`assigned_week` test-data gap + a documented, reproducible 17-failure baseline)**: investigating an unrelated empty-weekly-view test failure found the real cause — the real master sheet's Assigned Week column is literally named `"Week"`, not the fixed default `"Assigned Week"` header; a fresh ingest via the bare `POST /ingestion/load` (no AI column-mapping) leaves all 422 real vendors with `assigned_week = None`. Test fixed by seeding one vendor's `assigned_week` directly rather than depending on the real sheet's column layout. Broadened while investigating a stray "11 vs 17 failures" discrepancy: `backend/weekly_planning/test_planner.py` + `backend/shared/test_min_funds.py` + `backend/models/new_model_2/test_allocator.py` together give a stable, reproducible **17 failed / 42 passed** (confirmed 3x, not flaky) — full per-test root-cause table now lives in `docs/18-project-status-tracker.md` (9 share the `assigned_week` cause above, 4 are a missing `session=` kwarg tripping `conftest.py`'s real-DB-write guard, 2 are vendor-specific tranche-shape assumptions that drift as the real sheet grows, 1 surfaces a real open algorithmic question in `_apply_leftover_topup()`'s within-bucket parity).

**Resolved (Reset button — clears current cycle's calculations)**: new Planning-toolbar "Reset" button (right of Download Excel, disabled until a plan exists this cycle) via `POST /models/5/reset-cycle` — deletes the current planning month's `PlanRun`/`PlanAllocation` rows, reuses `backend/month_end/rollover.py::_reset_vendor_cycle_state()` verbatim (same reset month-end rollover already uses), writes one audit-log entry, all in one transaction. Never touches vendor master data/Excel, Configuration, already-finalized past months, or the confirmed planning month value — takes Finance back to exactly the post-upload, pre-Min-Funds state (rule 4). Real bug fixed along the way: `_resolve_planning_month()` used to always prefer the latest `PlanRun` over the stored `planning.current_planning_month` config value; after Reset deletes the current cycle's only row, that would resolve back to a stale older month instead of the fresh plan-less current one — now compares both by calendar month and takes whichever is later. Independently re-verified: 20 passed / 1 pre-existing unrelated failure in `test_new_model_2_router.py` (an earlier report's "22 passed, all green" and a claimed `PlanRunOut` fix were both corrected on re-check — see `docs/18-project-status-tracker.md`'s 2026-08-05 entry for the full reporting-accuracy trail).

**Resolved (Overridden-only filter, download filename timestamps, popup copy cleanup)**: Planning tab's Filters popover gained an "Overridden only" toggle (vendors with a real `override_amount` this cycle, ANDs with the existing Category/Aging/Priority Tag filters). All 3 Excel download endpoints (Min Funds Verification, Vendor Payment Plan, Vendor Analytics exports) now append the real download date+time to the filename, not just the planning month, so repeat downloads in one cycle no longer collide. Every Finance-facing popup/toast that leaked "New Model 2"/internal model naming was reworded (confirmed via a fresh repo-wide grep, zero remaining hits outside code comments), and raw exception strings in error toasts now route through a `friendlyErrorMessage()` helper instead of showing technical text verbatim.

**Resolved, with an important open caveat below (Configuration tab rebuilt — dynamic Category/Priority-Tag table)**: the old Priority Buckets table (with its confusing raw "Rotation Position" number) is now one unified, extensible table — Category name, Priority Tag, Ceiling %, Floor %, with the row's own position in the list acting as the cut order (drag-to-reorder, top = cut last, bottom = cut first). Must Pay/Commitment remain permanently fixed and non-editable per rule 5 — never appear as a row. Finance can now add a genuinely new category (e.g. "Contractor") through a "+ Add category" button; it's always cuttable, gets an auto-suggested Priority Tag, and flows through the same bucket-ceiling/cutting logic as Normal/Inactive. This went deeper than a UI wiring change: `Vendor.category` itself changed from a hardcoded 4-value SQLAlchemy `Enum` column to a plain `String` column (mirroring `priority_tag`'s existing precedent), and `PriorityBucket` gained its own `category_name` column so each row (P2-P5, or a new custom one) carries its own Finance-facing label. A real pre-existing bug was found and fixed along the way: the old `Enum` column physically stored the member NAME (`"MUST_PAY"`, `"NORMAL"`), not the `.value` (`"must_pay"`, `"normal"`) — a one-time `LOWER()` migration (`backend/db/session.py::_normalize_category_values()`, idempotent, runs on every `init_db()`) normalizes old rows. `allocator.py`'s `_is_guaranteed()` and `GET /vendors/categories` both confirmed (by direct code read) to genuinely read the live category set now, not a hardcoded enum.

**Not fully verified yet — needs redoing at the start of the next session, do this first**: independently verifying the report above surfaced a serious, still-unresolved problem — the real `backend/db/app.db` was found with an **empty `vendors` table**, and by the end of that same session, `priority_buckets` was empty too (both re-confirmed by direct query). The real master Excel (`data/Vendor's Details.xlsx`) is intact — 453 rows, so no actual data was lost, and this is a derived/rebuildable table per `docs/12-database.md`, not a second source of truth — but the DB itself needs a fresh ingestion run before it's trustworthy again. Root cause not established: the previous session's own report admitted "running pytest concurrently against the same real `app.db`" caused stray audit-log rows as a "process-hygiene mistake," which is the same risk class that could explain this too, but independent verification couldn't rule out its own test run contributing either — be honest about this uncertainty if it comes up again, don't assert a confident root cause without re-deriving it. **First thing to do next session**: (1) re-run ingestion against the real Excel file to repopulate `vendors` (this also reseeds `priority_buckets` to its P2-P5 defaults on first `list_buckets()` call), (2) re-run `backend/configuration/test_priority_bucket_edits.py` fresh against the restored DB — an interim run returned **5 failed, 16 passed**, but every failure looked like a symptom of the empty vendors table ("expected >=2 Normal vendors, found 0"), not necessarily a real code defect, so this needs a clean re-run before drawing any conclusion, (3) only after that, treat the earlier report's "70 failed / 398 passed, all pre-existing dataset drift" claim as unverified until re-checked against a healthy DB — it was measured on top of whatever state led to the emptiness, so it cannot be trusted as-is.

**Resolved (Planning toolbar one-row layout + Analytics font/weight parity)**: two small direct edits (no Claude Code prompt needed, done in-session). Planning tab's toolbar (Regenerate/Verify Min Funds/Finalize/Download Excel/Reset) was wrapping the Reset button to a second row — tightened button padding/gaps (`px-4 py-2`→`px-3 py-1.5`, `gap-2.5`→`gap-1.5`), switched `flex-wrap`→`flex-nowrap`, narrowed the search box, all 5 buttons now fit one row. Separately, `VendorAnalyticsTab.tsx` (built externally in Google AI Studio) visually didn't match the rest of the app — root cause was a stray `font-sans` on its top-level wrapper overriding the Inter font every other tab inherits from the app shell, plus the component using `font-normal` almost everywhere (Planning never goes below `font-medium` for real text). Fixed: removed every `font-sans` in the file (now correctly inherits Inter), and remapped every `font-normal` to match Planning's actual convention — `font-bold` for KPI headline numbers/panel titles, `font-semibold` for the table header row/money amounts/badges, `font-medium` for secondary labels/captions. `tsc --noEmit` clean.

**Not yet done — next up**: 2 UI/UX prompts delivered, not yet executed — `claude_code_prompt_planning_functional_fixes.md` (Finalize modal needs a proactive signed surplus/shortfall preview instead of a reactive one-sided red warning; weekly-distribution cards go stale after an override + regenerate) and `claude_code_prompt_ui_polish_pass.md` (filter chip colors don't match the app's green theme; Analytics page has unexplained empty side margins unlike every other tab; sidebar/site-wide rounded-corner polish pass — note the Analytics-margins and filter-chip-color items may already be partly addressed by the font-parity fix above and should be re-checked before re-running this prompt wholesale). An Analytics-tab-lock prompt (`claude_code_prompt_analytics_lock.md`) was also written earlier but is now moot — Analytics is fully built, per the resolved entry above — flagged to Sarath rather than executed.

**Resolved (Min Funds Required investigation + duplicate-vendor-row rule)**: Finance manually cross-checked Min Funds Required against a ~150-name vendor list and flagged several as wrong. Investigation (diagnosis only, no code changes at that stage) found the real cause wasn't a calculation bug: 14 of those vendor NAMES resolve to **two separate `Vendor` rows** under two different ERP codes (e.g. "G-Task Services" is both a ₹22.3L Commitment row and a ₹2,659 Must Pay row) — Finance was comparing one manual number against what the system actually splits across two rows. Confirmed with Finance: these 14 are genuinely two different billing lines, not data-entry duplicates, and must keep calculating independently. New, Finance-confirmed business rule implemented on top of that finding: `backend/shared/min_funds_v2.py::detect_duplicate_vendor_rows()` flags a same-vendor_name pair (case/whitespace-insensitive — matches the real sheet's trailing-space "Hopefloat Technologies Pvt Ltd " case) as a true duplicate **only** if opening_balance, category, commitment_months, paid_so_far_this_month, is_active, override_amount, AND the full monthly_ledger row set all match exactly; otherwise both rows stand alone, unchanged from today. `calculate_minimum_funds_required_v2()` (Card 2 + the Planning table's Total Min Funds column) now excludes a flagged duplicate's `required_amount` from `total` while still surfacing it in the breakdown via `is_duplicate`/`duplicate_of_vendor_id` — never silently dropped. `GET /models/5/min-funds-verification-export` got the same detection wired in as a "Duplicate Of ERP Code" column (informational — this export has no grand-total row to correct). Verified against the real DB: none of the 14 known pairs are flagged (test asserts this explicitly and fails loudly if that ever changes). Commitment's own formula/`required_amount()` deliberately untouched — out of scope, confirmed correct as-is. Full rule detail: `docs/14-new-model-2.md`'s new "Duplicate-vendor-row rule" section.

**Resolved (8 pre-existing test failures verified and fixed — two separate bug classes, not one)**: a follow-up investigation into 8 failures surfaced by the duplicate-vendor-row task above (5 in `test_allocator.py`, 3 in `test_upload.py`) confirmed all 8 were genuinely pre-existing and unrelated to that task (file-mtime evidence: `allocator.py`/`upload.py`/`conftest.py` untouched for hours before the duplicate-detection edit), then fixed both classes for real rather than leaving them documented-but-broken.

Class 1 — dataset-drift (4 tests, all in the existing [[demo15_dataset_quirks]]-documented family, now fixed by deriving instead of hardcoding a fresh number that would just drift again): `test_upload.py::test_commit_detects_new_vendor`'s `existing_vendor_count == 83` now queries the real `Vendor` count directly. `test_upload.py::test_commit_detects_changed_ledger_figure` and `test_commit_detects_unmapped_column` turned out to be masking a real, separate finding: a *pure openpyxl load+save round-trip of the real master sheet, with zero cell edits*, already shifts 5 unrelated vendors' derived fields — the sheet's Closing Balance column is a live Excel formula, and openpyxl's resave drops its cached value, which `load_excel.py`'s running-balance walk depends on for a handful of rows. Confirmed a test-fixture artifact only (every edit helper in this file must resave via openpyxl to make an edit; real Finance uploads are never an openpyxl round-trip of the existing master, so production is unaffected) — but it means the old hardcoded counts (`1`, `"VN"`) were really asserting "sheet size" *and* "how much resave noise this file produces today" at once. Fixed both by diffing against a resave-only control commit instead of a literal. `test_allocator.py::test_mixed_categories_generate_plan_respects_funds_and_tags_inactive_rows` (and, once Class 2 below was fixed, `test_topup_never_pushes_total_allocated_past_available_funds` too) asserted `total_allocated <= available_funds * ratio` — invalid now that the real DB's own Must Pay/Commitment volume (`guaranteed_total`) can exceed that figure on its own; per rule 5 guaranteed rows are exempt from the funds constraint entirely. Fixed by asserting guaranteed rows equal `plan["guaranteed_total"]` exactly and bounding only the Normal/Inactive portion against what's left for it, skipping that second bound when `plan["exceptional_shortfall"]` is true (rule 6 legitimately allows bucket floors to exceed remaining funds then). Full detail: [[demo15_dataset_quirks]]'s 2026-08-06 entry.

Class 2 — test-isolation gap, a different bug from Class 1, not filed under it: 4 `test_allocator.py` tests (`test_absolute_funds_gives_100_percent_not_ceiling`, `test_no_allocation_exceeds_outstanding_balance`, `test_topup_never_pushes_total_allocated_past_available_funds`, `test_topup_full_generate_plan_is_deterministic`) called `generate_plan()`/`calculate_minimum_funds_required_v2()` with no `session=` kwarg, defaulting to a real `SessionLocal()` against `app.db` — `backend/conftest.py`'s isolation guard caught the resulting real commit before anything actually wrote (this is the 4th occurrence of that same guardrail bug class catching a real mistake — see memory `feedback_test_isolation_guardrail.md`). Fixed by adding the missing `session=` + the same `try/finally: session.rollback(); session.close()` every sibling test in the file already uses.

All 8 originally-targeted tests confirmed passing individually against the repo state at the time of the fix. **Not a clean full-suite confirmation, flagged rather than glossed over**: another concurrent edit landed on `allocator.py`/`column_mapping.py`/`load_excel.py`/`test_allocator.py` (a P0/P1-ceiling + Configuration-tab category rework, mid-flight, not this task's own change) partway through this task's verification pass — file mtimes confirmed those files changed *after* this task's own edits and *after* an initial clean full-suite run had already started, making that run's 60-failed/428-passed result untrustworthy as a signal for this task specifically. Re-running the target files fresh afterward found the 8 target tests still green, plus 3 new unrelated failures in `test_allocator.py` (P6-bucket/no-priority-tag fallback tests, breaking on a `nan` instead of the expected fallback tag — symptomatic of that other in-flight rework, not touched here) and 2 new unrelated failures in `test_upload.py` (`test_commit_writes_audit_log_entries_with_excel_upload_source`, `test_commit_soft_deactivates_vendor_missing_from_upload`, both hinge on `V00400`'s category/ledger derivation via the same files that just changed). None of these 5 are in this task's scope or edit list — left for whoever's mid-edit on that other work to resolve.

**Resolved (duplicate-vendor-row rule removed — Sarath's explicit reversal, one duplicate check only)**: the same-vendor_name/all-fields-exact-match check added above (`detect_duplicate_vendor_rows()`) is gone — Sarath's call: there should be exactly **one** duplicate check in this codebase, keyed on ERP code only, not vendor name. `backend/shared/min_funds_v2.py::detect_duplicate_vendor_rows()`/`_vendor_duplicate_signature()`/`_normalize_vendor_name()` deleted outright, along with `is_duplicate`/`duplicate_of_vendor_id` from `calculate_minimum_funds_required_v2()`'s breakdown and the "Duplicate Of ERP Code" column from `GET /models/5/min-funds-verification-export` — `total` is back to a plain sum, no exclusion logic. The remaining (and now only) duplicate check, `backend/ingestion/load_excel.py`'s ERP-code check, also changed behavior in the same task: **first row wins, not last** (reversing the earlier "last-row-wins" decision from the 2026-07-31 investigation) — every row after the first occurrence of a repeated ERP code is now skipped entirely during `load()` (never read, never overwrites, never calculated against), rather than silently overwriting the earlier row. `grid.py`'s `duplicate_dropped_rows` (feeds the Master Data grid's red "Duplicate" badge + below-grid skipped-rows table) flipped to match (`rows[1:]` instead of `rows[:-1]`). Full rule: `docs/14-new-model-2.md`'s "Duplicate-vendor rule (ERP-code-only, revised 2026-08-06)" section.

**Above all of this — the empty-DB caveat two paragraphs up is the actual top priority.** Don't start new feature work until `vendors`/`priority_buckets` are confirmed repopulated and the Configuration rebuild's test suite has a clean, trustworthy re-run. **Update (this task, incidental confirmation while investigating Min Funds):** the real `app.db` is no longer empty — `vendors` has 422 active rows with real category/priority_tag data (confirmed by direct query during this task). The Configuration-rebuild test suite itself was not re-run here (out of scope for this task), so that half of the caveat still needs its own clean re-run before fully closing this out.

**Resolved (P0/P1-ceiling task — Must Pay/Commitment funding now configurable)**: rule 5's hardcoded "Must Pay/Commitment always fully funded, bypass the bucket math entirely" is gone. `allocator.py`'s old `_is_guaranteed()`/separate guaranteed-pool pre-funding step was replaced with two more `PriorityBucket` rows, `P0`/`P1`, seeded at ceiling=floor=100% by default and pinned permanently at `rotation_position` -2/-1 (ahead of P2's 0, so every existing P2-P5 row's position needed no renumbering, and a real DB that already had P2-P5 seeded gets P0/P1 backfilled on the next `_seed_and_read_buckets()` call, not just on a fresh empty table). Every vendor — P0/P1 included — now flows through the exact same effective-bucket/ceiling/floor/cutting/leftover-topup math; the OR-semantics guaranteed-tier check (category `must_pay`/`commitment` OR tag literally `P0`/`P1` — needed because the real sheet has Normal-category vendors tagged P0/P1 AND Must Pay/Commitment vendors with a stray/missing tag) moved to `PINNED_BUCKET_KEYS`/`_CATEGORY_DEFAULT_BUCKET`. At the untouched 100%/100% default this is byte-identical to the old behavior — proven via the full existing test suite (`test_allocator.py` 38/38, the 45-vendor e2e validation 41/41, `test_priority_bucket_edits.py`, `test_planner.py`) plus two new tests: one confirming the default reproduces the old hard guarantee exactly, one confirming an edited P0 floor actually cuts a Must Pay vendor under a severe shortfall. One real regression caught and fixed during this same task (not left as a "known gap"): `leftover_remaining`'s formula had to keep clamping the guaranteed tier's own shortfall to zero *before* subtracting what everyone else took (`max(available_funds − pinned_allocated, 0) − other_allocated`, not a flat `available_funds − everything`) — a naive whole-plan subtraction double-counted an escalation shortfall and changed `leftover_remaining` under severe shortfall even at the default config, which the "zero output diff at default" bar doesn't allow. A second regression, in a file this task didn't directly touch: `weekly_planning/planner.py`'s within-week execution-order rank hardcoded `{"P0": 0, "P1": 1}` then re-derived P2+ ranks by enumerating `bucket_order` — now that `bucket_order` itself also contains P0/P1, that enumeration silently overwrote the hardcoded 0/1 with 2/3. Fixed by filtering `PINNED_BUCKET_KEYS` out of that enumeration. Configuration tab: P0/P1 now render as real rows (category/tag/ceiling/floor), pinned (no drag handle, no Remove button, category name/tag not editable) — only ceiling/floor are Finance-editable inputs, same as every other row. `reorder_buckets()` now only accepts the reorderable subset (P2-P5-and-beyond); passing P0/P1 in that payload is rejected. The two help paragraphs describing the old hardcoded-and-not-configurable-here behavior were removed from the Categories card (per this task's own instruction) — nothing replaced them. Two pre-existing, unrelated test failures surfaced while verifying this (confirmed via isolated re-runs against code this task never touched — `min_funds.py`'s old `required_amount()` vs `min_funds_v2.py`'s `required_amount_v2()` formula divergence for a Must Pay vendor, and a stale test asserting the old required-`planning_month`-param contract a prior task already made optional) — left alone, not this task's scope. Full detail: `docs/14-new-model-2.md`'s "Vendor categories and priority tags"/"Bucket ceilings"/"Allocation logic" sections.

**Resolved (Analytics UI/UX fixes + Configuration P0/P1 edit-lock removed)**: `VendorAnalyticsTab.tsx` — the vendor detail modal now shows a 5-box Aging row (0-30/31-60/61-90/91-120/120+, each with its own amount) above the existing Monthly Breakdown table, reusing `AnalyticsVendor.aging_buckets` (already computed server-side, previously unused in the frontend). The Consumption vs Disbursal bar chart's value-label collision (two labels drawn centered on adjacent bars, overlapping whenever the bars are close in height) fixed by anchoring each dataset's label to its own bar's outer edge (`textAlign: right`/`left` + a few px offset) instead of centering both — collision-proof regardless of value proximity, not just a bigger gap. The "Oldest bucket concentration: N% of debt is over 120 days" line was removed from the Aging card (Sarath's call — added no value), along with its now-dead `kpis.pct120Plus`/`total120Plus` computation. The vendors table's "Ledger N entries" label and "Refresh Data" button were removed from the table header entirely (Sarath's call); the table itself was decluttered — removed the per-cell vertical border on every td/th (kept only at real group boundaries: after the frozen Name/Balance columns, between each month's Payable/Paid pair, before the Aging section), removed the per-cell green background tinting on Payable/Paid amounts (was creating a checkerboard look), and added row-level zebra striping (`bg-slate-50/50` on odd rows) matching `MasterDataGrid.tsx`'s existing convention instead of the flat all-white rows it had before.

Configuration tab's Must Pay/Commitment (P0/P1) edit-lock — CLAUDE.md rule 5's own "bucket_key/category/position can't be changed" clause — was removed at Sarath's explicit request (asked twice, confirmed both times after being shown the risk): `priority_bucket_edits.py`'s `update_bucket()`/`remove_bucket()`/`reorder_buckets()` no longer special-case `PINNED_BUCKET_KEYS` at all — P0/P1 can now be renamed, recategorized, reordered, and removed exactly like any other bucket row (subject to the same generic in-use guard on removal every row already had). `ConfigurationTab.tsx`'s padlock icon/greyed-out cells for these two rows are gone — they render as ordinary draggable, fully-editable rows now. **Known, accepted sharp edge, not fixed by this task**: `allocator.py`'s guaranteed-funding-tier detection (`PINNED_BUCKET_KEYS`, `_CATEGORY_DEFAULT_BUCKET`) still keys off the literal strings `"P0"`/`"P1"`/`"must_pay"`/`"commitment"`, not row identity — renaming bucket_key away from `"P0"` or category_name away from `"must_pay"` silently drops the affected vendors out of the guaranteed tier with no separate warning (real-money impact). Also, `_seed_and_read_buckets()` will re-seed a fresh default `"P0"`/`"P1"` row the next time buckets are read if the original key was renamed away, since its own existing-key check no longer finds `"P0"` — meaning a rename currently produces a phantom duplicate default row rather than a clean rename. Both were flagged to Sarath before implementing (twice, via clarifying questions) — he chose "unlock everything, no extra guard" over decoupling the literal-string dependency, so this is a deliberate, known state, not an oversight. Also updated Configuration's overall look (Sarath's "make this page intuitive, use theme colors" ask): added an icon-badge header for the page title and the Audit log card (matching Planning/Analytics' circle-icon convention), switched both tables' header rows from plain gray to the app's emerald/`#107c41` theme (matching Planning's table headers), added row hover highlighting and focus-ring states on every input/select/date field that was missing one.

**Resolved (Audit log revamp — collapsed upload noise, 5 new action types, filter, table width)**: `backend/ingestion/upload.py` no longer writes one `AuditLog` row per vendor per Excel upload (400+ rows for a full re-upload) — collapsed into one summary row ("N vendors processed — X new, Y changed, Z removed"). Five action types that either weren't logged at all or shared the generic `ui_edit` source now get their own dedicated `ChangeSource` value: `generate_plan`, `reset_cycle` (Reset Cycle was already logged, just under `ui_edit` — resolved to its own source), `export` (all three download endpoints — Min Funds Verification, Finalized Plan, Vendor Analytics — each naming which type), `funds_input` (the Available Funds figure, logged alongside Generate Plan since there's no separate "submit funds" endpoint in this codebase), and `min_funds_calc` (Card 2's explicit "Calculate Minimum Funds Required" click only — deliberately NOT the Planning table's own much-more-frequent per-vendor Min Funds column, which stays unlogged). Configuration tab's existing source-filter dropdown gained all five as options. `ConfigurationTab.tsx`'s outer wrapper lost its `max-w-5xl mx-auto` cap (was narrower/centered than every other tab, including Planning's own tables) — now `w-full` like the rest of the app.

## Docs index

| File                                          | Contents                                                                                                                                             |
| --------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| `docs/01-objective-and-current-process.md`  | Why this exists, what Finance does manually today, success metrics                                                                                   |
| `docs/02-architecture.md`                   | Deterministic + AI layer split, step-by-step system flow, stack rationale                                                                            |
| `docs/06-weekly-planning-regeneration.md`   | W1-W5 split, Assigned Week tag, Reconsider flag, regeneration rules                                                                                  |
| `docs/07-data-pipeline-and-master-sheet.md` | Master Excel structure, ingestion, daily manual payment entry, ledger integrity formula                                                              |
| `docs/08-scope.md`                          | In scope / out of scope for this build                                                                                                               |
| `docs/9-open-questions.md`                  | Unresolved items — don't guess these, ask                                                                                                           |
| `docs/11-configuration-module.md`           | Vendor/tag edits + system config, Excel write-back rule, audit trail                                                                                 |
| `docs/12-database.md`                       | SQLite decision, reasoning, rough table shape                                                                                                        |
| `docs/14-new-model-2.md`                    | New Model 2 — the single planning model: categories/tags, bucket ceilings, minimum funds, allocation logic, leftover top-up, overrides, weekly view |
| `docs/15-react-ui-layout.md`                | React UI application shell — collapsible sidebar, 4-tab layout, tab 2's card/filter/table structure, stack + mock-data-phase decisions              |
| `docs/16-endpoint-ui-mapping.md`             | Every backend endpoint mapped to its React caller (or flagged as unmapped, with why) — `/rollover` and `/audit-log` have no UI entry point           |
| `docs/17-frontend-hardcoded-values-audit.md` | CLAUDE.md rule 7 audit of `frontend/` — what's a legitimate fixed constant vs. an actual should-come-from-backend gap                                 |
| `docs/18-project-status-tracker.md`          | Priority-ordered running checklist — what's done, what's ready to execute, what's pending, what needs Sarath's decision. Check this before starting new work. Has an OPEN INCIDENT section at the end (empty `vendors`/`priority_buckets` in real `app.db`) — read that first. |
| `docs/19-handoff-2026-08-06.md`               | Session handoff doc — read this first in a new session if picking up mid-project. Working conventions, current state summary, and the top-priority open DB incident. |

## Working conventions

- This is a fresh project — there is no prior code or data available anywhere for this repo. Do not assume an existing MVP's code can be found or ported; implement from the specs in `docs/` directly. Where a spec leaves a formula detail genuinely undecided (e.g., exact aging bucket boundaries, which direction a factor should push a score), implement a clearly documented, reasonable default and flag it in the code and to Sarath for confirmation — don't block waiting for an answer, and don't silently guess without flagging it either.
- Prefer plain, readable Python for the deterministic layer — this code needs to be auditable by non-engineers (Finance, leadership) if questioned.
- Keep the AI layer's code physically separate from the deterministic layer's code (e.g., separate module/package) so the "never touches raw data" rule is enforceable by code review, not just convention.
- **Keep code minimal — use libraries, don't hand-roll.** For a small, well-defined task, prefer a well-maintained library that already solves it (e.g., pandas for data manipulation and Excel I/O, standard library `datetime`/`decimal` for date and money math) over writing custom logic from scratch. Don't add abstraction layers, wrapper classes, or configuration flexibility for needs that aren't actually specified yet. If implementing something the docs describe as simple is turning into a lot of code, stop — check whether a library already does this, or whether the approach is over-engineered, before continuing.
